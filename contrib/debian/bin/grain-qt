#!/bin/sh

set -e

umask 077

basedir=~/.Grain
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/Grain.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# Grain does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/Grain/grain-qt "$@"
